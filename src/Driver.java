import java.util.LinkedList;
import java.util.Scanner;

public class Driver {

	//final String name = null; 
	//static Scanner input = new Scanner(System.in);

	public static void main (String[] args)
	{

		GamePiece Tank = GamePiece.RED_THIMBLE; 
		
		/*LinkedList<String> Players = new LinkedList<String>(); 


		System.out.println("Please enter your first name with no spaces");
		String name = input.nextLine();
		Players.add(name.toLowerCase()); 
		System.out.println(name + ", please choose from the list by entering the numbers in brackets");
		System.out.println("[1] ADD [2] MOVE LOCATIONS [3] REQUEST INFORMATION OF THE BOARD AND ALL PLAYERS or [4] QUIT THE GAME");


		String nextMove = input.nextLine();
		Boolean brake = false; 
		while (!(name.equalsIgnoreCase("[4]")) && brake.equals(false)){

			brake.equals(true); 
			System.out.println("Made into while and changed boolean");
			//addPlayer(playerName, gamePiece, intialLocation, Location); 


			if (nextMove.equalsIgnoreCase("[1]")){
				for (int i = 0; i < Players.size(); i++)
				{
					String additionalPlayer = input.nextLine(); 
					Players.add(additionalPlayer.toLowerCase()); 
					//if two names match each other
				}
				System.out.println("Please enter your first name with no spaces");

				System.out.println(Players);
				brake.equals(false); 
				//nextMove = input.nextLine(); 
			}

			else if (nextMove.equalsIgnoreCase("[2]")){
				//name.getGamePiece and move
				//CALL METHOD TO MOVE PIECE
			}
			else if (nextMove.equalsIgnoreCase("[3]")){
				//cycle through all the other players - create an array to store the players in and cycle through, 
				//most likely having to adjust the current player to the first position so not an array but rather a linked list
				//perhaps
				//set a boolean to break out of this else if and then if they want to move or quit or something else allows them
				//to without taking up their turn
			}


			else if(nextMove.equalsIgnoreCase("[4]"))
			{
				System.out.println("Thanks for playing!");
				brake.equals(true); 
			}

			else {
				System.out.println("That is not a valid command - please play again");
				brake.equals(false); 
				break; 
			}

		}}
		 */
	}
}